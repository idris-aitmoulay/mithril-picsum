import {
  Typography
} from '../../shared/components';
const HomePage = () => {
  return {
    view: () => (
        <div>
          <Typography variant={"h1"}>live</Typography>
        </div>
      )
  };
};

export default HomePage;
